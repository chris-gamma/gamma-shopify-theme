# Merchant Impact Checklist

Use this as working notes while making a schema or template migration.

## Contract

- Affected section or block:
- Affected template JSON files:
- New settings:
- Removed settings:
- Renamed settings:

## Static Block Sync

- `content_for 'block'` IDs or types changed:
- Matching JSON nodes updated:
- Nested `blocks` trees checked:
- `block_order` checked:

## Locales

- Schema locale keys added or changed:
- Storefront locale keys added or changed:
- Existing keys reused where possible:

## Merchant Impact

- Existing configurations preserved:
- Default behavior changed:
- Manual editor follow-up required:
- Risk to saved section instances or block trees:

## Validation

- `shopify theme check`:
- Dev MCP theme validation:
- `shopify theme dev` storefront verification:
- Theme editor verification:
- `shopify theme profile` or Theme Inspector if relevant: